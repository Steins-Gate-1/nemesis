## Packages
recharts | For risk meters, attack path visualizations, and statistical dashboards
date-fns | For formatting breach dates, timestamps, and audit logs
framer-motion | For cinematic, terminal-style entry animations and tactical UI reveals

## Notes
The application forces a dark mode, military-grade terminal aesthetic.
Ensure all API calls use the defined Zod schemas from `@shared/routes` for validation.
Background grid patterns and scanlines are implemented via pure CSS in `index.css`.
